  import 'package:flutter/material.dart';

  class AppHeatmapWidget extends StatelessWidget {
    const AppHeatmapWidget({super.key});

    @override
    Widget build(BuildContext context) {
      return const Placeholder();
    }
  }