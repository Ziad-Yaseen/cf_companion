import 'package:flutter/material.dart';

class ProblemsetScreen extends StatelessWidget {
  const ProblemsetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}